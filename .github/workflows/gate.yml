# Baseline gate per DEVSECOPS-GATES-BASELINE.md: restore + build + unit tests + integration tests (host + UI contract)
# Runs on PRs and push to dev. Integration tests use LocalDb (windows-latest) and validate host startup and UI-like flows.
# Status check name for branch protection: gate
name: Gate

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main, dev]

concurrency:
  group: gate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/*.sln') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-incremental

      - name: Test (unit)
        run: |
          dotnet test ./Atlas.Api.Tests/Atlas.Api.Tests.csproj -c Release --no-build
          dotnet test ./Atlas.DbMigrator.Tests/Atlas.DbMigrator.Tests.csproj -c Release --no-build
      - name: Test (integration, host + UI contract)
        run: dotnet test ./Atlas.Api.IntegrationTests/Atlas.Api.IntegrationTests.csproj -c Release --no-build
